# Local build/deploy scratch directories
_site/
_resume/

# CV is copied from the private resume repository during GitHub Actions deployment.
data/resume.pdf

# Avoid accidentally re-adding a local resume clone/submodule.
resume/

# Avoid accidentally committing the old private homepage clone.
mindorigin150.github.io-legacy/

# OS/editor noise
.DS_Store
